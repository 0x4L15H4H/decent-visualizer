# Local dev only. The backend reads these two secrets from here; domain and
# cloudflare_pages_project come from ../config.env, and the CORS origins are
# derived from those, so there is nothing CORS-related to set.
supabase_url=https://your-project.supabase.co
supabase_service_key=your-service-role-key
